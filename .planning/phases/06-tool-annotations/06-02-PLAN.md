---
phase: 06-tool-annotations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mcp-provider-mobile-web/src/tools/offline-analysis/get_mobile_lwc_offline_analysis.ts
  - packages/mcp-provider-mobile-web/src/tools/offline-guidance/get_mobile_lwc_offline_guidance.ts
  - packages/mcp-provider-mobile-web/src/tools/native-capabilities/create_mobile_lwc_native_capabilities.ts
  - packages/mcp-provider-code-analyzer/src/tools/run_code_analyzer.ts
  - packages/mcp-provider-code-analyzer/src/tools/list_code_analyzer_rules.ts
  - packages/mcp-provider-code-analyzer/src/tools/describe_code_analyzer_rule.ts
  - packages/mcp-provider-code-analyzer/src/tools/query_code_analyzer_results.ts
  - packages/mcp-provider-code-analyzer/src/tools/generate_xpath_prompt.ts
  - packages/mcp-provider-devops/src/tools/sfDevopsDetectConflict.ts
  - packages/mcp-provider-devops/src/tools/sfDevopsResolveConflict.ts
  - packages/mcp-provider-scale-products/src/tools/scan-apex-antipatterns-tool.ts
  - packages/mcp/src/tools/enable_tools.ts
  - packages/mcp/src/tools/list_tools.ts
autonomous: true
requirements:
  - META-01
  - META-02

must_haves:
  truths:
    - "Every GA tool in mobile-web, code-analyzer, devops, scale-products, and mcp packages has all four hints"
    - "run_code_analyzer has readOnlyHint: true (fixing existing bug — it was incorrectly set to false)"
    - "get_ast_nodes_to_generate_xpath has readOnlyHint: true (fixing existing bug — it was incorrectly set to false)"
    - "detect_devops_center_merge_conflict and resolve_devops_center_merge_conflict have annotations blocks added"
  artifacts:
    - path: "packages/mcp-provider-devops/src/tools/sfDevopsDetectConflict.ts"
      provides: "annotations block added (previously missing entirely)"
      contains: "readOnlyHint: true"
    - path: "packages/mcp-provider-devops/src/tools/sfDevopsResolveConflict.ts"
      provides: "annotations block added (previously missing entirely)"
      contains: "readOnlyHint: false"
    - path: "packages/mcp-provider-code-analyzer/src/tools/run_code_analyzer.ts"
      provides: "readOnlyHint bug fixed from false to true"
      contains: "readOnlyHint: true"
  key_links:
    - from: "tool-categories.ts"
      to: "run_code_analyzer / get_ast_nodes_to_generate_xpath annotations"
      via: "category is 'read' so readOnlyHint must be true (was incorrectly false)"
      pattern: "readOnlyHint: true"
---

<objective>
Complete all four MCP tool annotation hints on every GA tool in the mobile-web, code-analyzer, devops, scale-products, and mcp packages. Fix two existing incorrect readOnlyHint values (run_code_analyzer and get_ast_nodes_to_generate_xpath are classified as 'read' but had readOnlyHint: false).

Purpose: Eliminates false confirmation dialogs on read-only analysis tools. Adds the two devops tools' missing annotations blocks. Fills idempotentHint gaps universally.

Output: 13 tool files across 5 packages each with complete 4-hint annotations blocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/mcp/src/utils/tool-categories.ts
</context>

<interfaces>
<!-- tool-categories.ts classification for these packages' GA tools: -->
<!-- read: get_mobile_lwc_offline_analysis, get_mobile_lwc_offline_guidance, create_mobile_lwc_* (13 native capability tools) -->
<!-- read: run_code_analyzer, list_code_analyzer_rules, describe_code_analyzer_rule, query_code_analyzer_results, get_ast_nodes_to_generate_xpath -->
<!-- read: detect_devops_center_merge_conflict -->
<!-- write: resolve_devops_center_merge_conflict -->
<!-- read: scan_apex_class_for_antipatterns -->
<!-- enable_tools and list_tools are NOT in tool-categories.ts (default is 'write') but use readOnlyHint: true intentionally -->

<!-- Annotation type (all booleans, all optional): -->
```typescript
annotations?: {
  readOnlyHint?: boolean;    // true = no side-effects on any external system
  destructiveHint?: boolean; // true = may cause irreversible data loss
  idempotentHint?: boolean;  // true = calling twice has same effect as once
  openWorldHint?: boolean;   // true = interacts with external resources beyond this org
}
```

<!-- NativeCapabilityTool is a SINGLE class in create_mobile_lwc_native_capabilities.ts -->
<!-- that serves all 13 create_mobile_lwc_* tool IDs. One annotation block covers all 13. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Complete annotations on mobile-web GA tools</name>
  <files>
    packages/mcp-provider-mobile-web/src/tools/offline-analysis/get_mobile_lwc_offline_analysis.ts
    packages/mcp-provider-mobile-web/src/tools/offline-guidance/get_mobile_lwc_offline_guidance.ts
    packages/mcp-provider-mobile-web/src/tools/native-capabilities/create_mobile_lwc_native_capabilities.ts
  </files>
  <read_first>
    packages/mcp-provider-mobile-web/src/tools/offline-analysis/get_mobile_lwc_offline_analysis.ts
    packages/mcp-provider-mobile-web/src/tools/offline-guidance/get_mobile_lwc_offline_guidance.ts
    packages/mcp-provider-mobile-web/src/tools/native-capabilities/create_mobile_lwc_native_capabilities.ts
  </read_first>
  <action>
In each file, locate the `annotations:` block inside `getConfig()` and replace it with the complete 4-hint set:

**get_mobile_lwc_offline_analysis.ts** (category: read — analyzes code locally, no org calls):
```typescript
annotations: {
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: true,
  openWorldHint: false,
},
```

**get_mobile_lwc_offline_guidance.ts** (category: read — returns static guidance, pure read):
```typescript
annotations: {
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: true,
  openWorldHint: false,
},
```

**create_mobile_lwc_native_capabilities.ts** (category: read — reads local resource files, returns type definitions; covers all 13 create_mobile_lwc_* tool IDs):
```typescript
annotations: {
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: true,
  openWorldHint: false,
},
```

Replace the existing `annotations: { readOnlyHint: true, }` block with the full 4-hint version in each file.
  </action>
  <verify>
    <automated>grep -c "idempotentHint" packages/mcp-provider-mobile-web/src/tools/offline-analysis/get_mobile_lwc_offline_analysis.ts packages/mcp-provider-mobile-web/src/tools/offline-guidance/get_mobile_lwc_offline_guidance.ts packages/mcp-provider-mobile-web/src/tools/native-capabilities/create_mobile_lwc_native_capabilities.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files contain all 4 annotation hints
    - All 3 files have readOnlyHint: true, openWorldHint: false
    - grep "idempotentHint" returns 1 match in each file
  </acceptance_criteria>
  <done>All mobile-web GA tools have complete 4-hint annotations</done>
</task>

<task type="auto">
  <name>Task 2: Fix and complete annotations on code-analyzer GA tools</name>
  <files>
    packages/mcp-provider-code-analyzer/src/tools/run_code_analyzer.ts
    packages/mcp-provider-code-analyzer/src/tools/list_code_analyzer_rules.ts
    packages/mcp-provider-code-analyzer/src/tools/describe_code_analyzer_rule.ts
    packages/mcp-provider-code-analyzer/src/tools/query_code_analyzer_results.ts
    packages/mcp-provider-code-analyzer/src/tools/generate_xpath_prompt.ts
  </files>
  <read_first>
    packages/mcp-provider-code-analyzer/src/tools/run_code_analyzer.ts
    packages/mcp-provider-code-analyzer/src/tools/list_code_analyzer_rules.ts
    packages/mcp-provider-code-analyzer/src/tools/describe_code_analyzer_rule.ts
    packages/mcp-provider-code-analyzer/src/tools/query_code_analyzer_results.ts
    packages/mcp-provider-code-analyzer/src/tools/generate_xpath_prompt.ts
    packages/mcp/src/utils/tool-categories.ts
  </read_first>
  <action>
CRITICAL: run_code_analyzer currently has `readOnlyHint: false` which is WRONG. The tool is classified as 'read' in tool-categories.ts. It writes a temp results file to disk but does not modify any Salesforce org state. Fix readOnlyHint to true.

CRITICAL: generate_xpath_prompt (get_ast_nodes_to_generate_xpath in tool-categories.ts) also has `readOnlyHint: false` incorrectly. It creates temp files but cleans them up automatically. Fix readOnlyHint to true.

Replace the `annotations:` block in each file:

**run_code_analyzer.ts** (category: read — writes temp results file but is stateless re: org; FIXING readOnlyHint false → true):
```typescript
annotations: {
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: false,
  openWorldHint: false,
},
```

**list_code_analyzer_rules.ts** (category: read — returns rule metadata, pure read):
```typescript
annotations: {
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: true,
  openWorldHint: false,
},
```

**describe_code_analyzer_rule.ts** (category: read — returns rule detail, pure read):
```typescript
annotations: {
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: true,
  openWorldHint: false,
},
```

**query_code_analyzer_results.ts** (category: read — reads existing results file, pure read):
```typescript
annotations: {
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: true,
  openWorldHint: false,
},
```

**generate_xpath_prompt.ts** (category: read — temp files are cleaned up; FIXING readOnlyHint false → true):
```typescript
annotations: {
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: false,
  openWorldHint: false,
},
```

Replace the entire `annotations: { ... }` block in each file's `getConfig()` return value.
  </action>
  <verify>
    <automated>grep -A 6 "annotations:" packages/mcp-provider-code-analyzer/src/tools/run_code_analyzer.ts packages/mcp-provider-code-analyzer/src/tools/generate_xpath_prompt.ts</automated>
  </verify>
  <acceptance_criteria>
    - run_code_analyzer.ts has readOnlyHint: true (was false — this is the key fix)
    - generate_xpath_prompt.ts has readOnlyHint: true (was false — this is the key fix)
    - All 5 code-analyzer files contain all 4 annotation keys
    - grep "readOnlyHint: false" in all 5 code-analyzer tool files returns zero matches
  </acceptance_criteria>
  <done>All 5 code-analyzer GA tools have complete 4-hint annotations with correct readOnlyHint values</done>
</task>

<task type="auto">
  <name>Task 3: Add annotations to devops GA tools and complete scale-products and mcp internal tools</name>
  <files>
    packages/mcp-provider-devops/src/tools/sfDevopsDetectConflict.ts
    packages/mcp-provider-devops/src/tools/sfDevopsResolveConflict.ts
    packages/mcp-provider-scale-products/src/tools/scan-apex-antipatterns-tool.ts
    packages/mcp/src/tools/enable_tools.ts
    packages/mcp/src/tools/list_tools.ts
  </files>
  <read_first>
    packages/mcp-provider-devops/src/tools/sfDevopsDetectConflict.ts
    packages/mcp-provider-devops/src/tools/sfDevopsResolveConflict.ts
    packages/mcp-provider-scale-products/src/tools/scan-apex-antipatterns-tool.ts
    packages/mcp/src/tools/enable_tools.ts
    packages/mcp/src/tools/list_tools.ts
  </read_first>
  <action>
DEVOPS TOOLS: Both devops GA tools currently have NO `annotations:` key at all. Add the key to the `getConfig()` return object, after `outputSchema: undefined,`.

**sfDevopsDetectConflict.ts** (getName() = "detect_devops_center_merge_conflict"; category: read — detects conflicts, reads git state; openWorldHint:true because it calls DevOps Center API):
In the return object of `getConfig()`, after the last existing key (`outputSchema: undefined,`), add:
```typescript
      annotations: {
        readOnlyHint: true,
        destructiveHint: false,
        idempotentHint: true,
        openWorldHint: true,
      },
```

**sfDevopsResolveConflict.ts** (getName() = "resolve_devops_center_merge_conflict"; category: write — modifies git state; NOT idempotent; openWorldHint:true because it calls DevOps Center API):
In the return object of `getConfig()`, after `outputSchema: undefined,`, add:
```typescript
      annotations: {
        readOnlyHint: false,
        destructiveHint: false,
        idempotentHint: false,
        openWorldHint: true,
      },
```

SCALE-PRODUCTS:
**scan-apex-antipatterns-tool.ts** (category: read — scans local file, no org calls; already has readOnlyHint: true):
Replace the partial `annotations: { readOnlyHint: true, }` block with:
```typescript
      annotations: {
        readOnlyHint: true,
        destructiveHint: false,
        idempotentHint: true,
        openWorldHint: false,
      },
```

MCP INTERNAL TOOLS:
**enable_tools.ts** (enables tools in server registry — readOnlyHint: true is intentional; not in tool-categories.ts):
Replace the partial annotations with:
```typescript
      annotations: {
        readOnlyHint: true,
        destructiveHint: false,
        idempotentHint: true,
        openWorldHint: false,
      },
```
Note: remove the `title:` field from inside annotations if present — it belongs in the config root, not annotations.

**list_tools.ts** (lists tools — pure read; not in tool-categories.ts):
Replace the partial annotations with:
```typescript
      annotations: {
        readOnlyHint: true,
        destructiveHint: false,
        idempotentHint: true,
        openWorldHint: false,
      },
```
  </action>
  <verify>
    <automated>grep -rn "idempotentHint" packages/mcp-provider-devops/src/tools/sfDevopsDetectConflict.ts packages/mcp-provider-devops/src/tools/sfDevopsResolveConflict.ts packages/mcp-provider-scale-products/src/tools/scan-apex-antipatterns-tool.ts packages/mcp/src/tools/enable_tools.ts packages/mcp/src/tools/list_tools.ts</automated>
  </verify>
  <acceptance_criteria>
    - sfDevopsDetectConflict.ts now contains an `annotations:` block with all 4 hints
    - sfDevopsResolveConflict.ts now contains an `annotations:` block with all 4 hints
    - sfDevopsDetectConflict.ts has readOnlyHint: true (category: read)
    - sfDevopsResolveConflict.ts has readOnlyHint: false (category: write)
    - scan-apex-antipatterns-tool.ts has all 4 hints with readOnlyHint: true
    - enable_tools.ts and list_tools.ts both have all 4 hints
    - No `title:` key inside any annotations block (title belongs at config root level)
  </acceptance_criteria>
  <done>All remaining GA tools across devops, scale-products, and mcp packages have complete 4-hint annotations</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotation values → LLM client | Client reads hints to decide confirmation dialogs; incorrect values mislead agent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | readOnlyHint on code-analyzer tools | mitigate | Plan 03 consistency test will catch any regression where read tool gets readOnlyHint: false |
| T-06-02-02 | Information Disclosure | Missing annotations on devops tools | mitigate | Adding annotations block eliminates undefined behavior in clients that assume defaults |
</threat_model>

<verification>
After completing all three tasks, verify across all modified packages:
```bash
grep -rn "idempotentHint" \
  packages/mcp-provider-mobile-web/src/tools/ \
  packages/mcp-provider-code-analyzer/src/tools/ \
  packages/mcp-provider-devops/src/tools/sfDevopsDetectConflict.ts \
  packages/mcp-provider-devops/src/tools/sfDevopsResolveConflict.ts \
  packages/mcp-provider-scale-products/src/tools/ \
  packages/mcp/src/tools/
```
Must return at least 10 lines (one per GA tool file modified in this plan).

Verify the two critical bug fixes:
```bash
grep -n "readOnlyHint" packages/mcp-provider-code-analyzer/src/tools/run_code_analyzer.ts
grep -n "readOnlyHint" packages/mcp-provider-code-analyzer/src/tools/generate_xpath_prompt.ts
```
Both must show `readOnlyHint: true`.
</verification>

<success_criteria>
- All 13 tool files modified have complete 4-hint annotation blocks
- run_code_analyzer.ts shows readOnlyHint: true (was incorrectly false)
- generate_xpath_prompt.ts shows readOnlyHint: true (was incorrectly false)
- sfDevopsDetectConflict.ts and sfDevopsResolveConflict.ts both have new annotations blocks
- TypeScript compiles without errors across all modified packages
</success_criteria>

<output>
After completion, create `.planning/phases/06-tool-annotations/06-02-SUMMARY.md`
</output>
