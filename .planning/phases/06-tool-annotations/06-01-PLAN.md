---
phase: 06-tool-annotations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mcp-provider-dx-core/src/tools/run_soql_query.ts
  - packages/mcp-provider-dx-core/src/tools/assign_permission_set.ts
  - packages/mcp-provider-dx-core/src/tools/run_apex_test.ts
  - packages/mcp-provider-dx-core/src/tools/run_agent_test.ts
  - packages/mcp-provider-dx-core/src/tools/list_all_orgs.ts
  - packages/mcp-provider-dx-core/src/tools/get_username.ts
  - packages/mcp-provider-dx-core/src/tools/resume_tool_operation.ts
  - packages/mcp-provider-dx-core/src/tools/deploy_metadata.ts
  - packages/mcp-provider-dx-core/src/tools/retrieve_metadata.ts
  - packages/mcp-provider-dx-core/src/tools/get_org_info.ts
autonomous: true
requirements:
  - META-01
  - META-02

must_haves:
  truths:
    - "Every dx-core GA tool has all four hints: readOnlyHint, destructiveHint, idempotentHint, openWorldHint"
    - "read-category tools have readOnlyHint: true; write-category tools have readOnlyHint: false"
    - "No hint is undefined or missing from any GA tool's annotations block"
  artifacts:
    - path: "packages/mcp-provider-dx-core/src/tools/run_soql_query.ts"
      provides: "Complete 4-hint annotations"
      contains: "idempotentHint"
    - path: "packages/mcp-provider-dx-core/src/tools/deploy_metadata.ts"
      provides: "Complete 4-hint annotations with readOnlyHint: false"
      contains: "readOnlyHint: false"
    - path: "packages/mcp-provider-dx-core/src/tools/assign_permission_set.ts"
      provides: "Complete 4-hint annotations with readOnlyHint: false"
      contains: "readOnlyHint: false"
  key_links:
    - from: "tool-categories.ts"
      to: "dx-core tool annotations"
      via: "readOnlyHint value must match 'read' -> true, 'write'/'execute' -> false"
      pattern: "readOnlyHint"
---

<objective>
Complete all four MCP tool annotation hints on every GA tool in the mcp-provider-dx-core package.

Purpose: LLM clients read these annotations to decide whether to show confirmation dialogs. Missing or incorrect readOnlyHint causes false write-confirmation prompts on read tools like run_soql_query. Missing idempotentHint and destructiveHint degrade agent decision quality.

Output: 10 dx-core tool files each with a complete `annotations` block containing exactly `readOnlyHint`, `destructiveHint`, `idempotentHint`, and `openWorldHint`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/mcp/src/utils/tool-categories.ts
</context>

<interfaces>
<!-- Annotation type from @modelcontextprotocol/sdk — all four hints are optional booleans -->
<!-- McpToolConfig.annotations shape (from SDK types): -->
```typescript
annotations?: {
  title?: string;
  readOnlyHint?: boolean;    // true = tool only reads, no side-effects (default: false)
  destructiveHint?: boolean; // true = may cause irreversible data loss (default: true)
  idempotentHint?: boolean;  // true = same input always same output (default: false)
  openWorldHint?: boolean;   // true = may interact with external world beyond org (default: true)
}
```

<!-- tool-categories.ts classification for dx-core GA tools: -->
<!-- read:  run_soql_query, get_username, list_all_orgs, retrieve_metadata, run_apex_test, run_agent_test, resume_tool_operation -->
<!-- write: deploy_metadata, assign_permission_set -->
<!-- salesforce_get_org_info is classified as 'read' in tool-categories.ts -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Complete annotations on dx-core read-category GA tools</name>
  <files>
    packages/mcp-provider-dx-core/src/tools/run_soql_query.ts
    packages/mcp-provider-dx-core/src/tools/list_all_orgs.ts
    packages/mcp-provider-dx-core/src/tools/get_username.ts
    packages/mcp-provider-dx-core/src/tools/run_apex_test.ts
    packages/mcp-provider-dx-core/src/tools/run_agent_test.ts
    packages/mcp-provider-dx-core/src/tools/resume_tool_operation.ts
    packages/mcp-provider-dx-core/src/tools/retrieve_metadata.ts
    packages/mcp-provider-dx-core/src/tools/get_org_info.ts
  </files>
  <read_first>
    packages/mcp-provider-dx-core/src/tools/run_soql_query.ts
    packages/mcp-provider-dx-core/src/tools/list_all_orgs.ts
    packages/mcp-provider-dx-core/src/tools/get_username.ts
    packages/mcp-provider-dx-core/src/tools/run_apex_test.ts
    packages/mcp-provider-dx-core/src/tools/run_agent_test.ts
    packages/mcp-provider-dx-core/src/tools/resume_tool_operation.ts
    packages/mcp-provider-dx-core/src/tools/retrieve_metadata.ts
    packages/mcp-provider-dx-core/src/tools/get_org_info.ts
    packages/mcp/src/utils/tool-categories.ts
  </read_first>
  <action>
For each of the 8 files listed, locate the `annotations:` block inside `getConfig()` and replace it with the complete 4-hint set. Use these exact values — do NOT deviate:

**run_soql_query.ts** (category: read — only reads data from org):
```typescript
annotations: {
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: true,
  openWorldHint: false,
},
```

**list_all_orgs.ts** (category: read — lists cached local config):
```typescript
annotations: {
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: true,
  openWorldHint: false,
},
```

**get_username.ts** (category: read — reads config, no side-effects):
```typescript
annotations: {
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: true,
  openWorldHint: false,
},
```

**run_apex_test.ts** (category: read — runs tests but does not mutate data; openWorldHint:true because it calls org API):
```typescript
annotations: {
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: false,
  openWorldHint: true,
},
```

**run_agent_test.ts** (category: read — same reasoning as run_apex_test):
```typescript
annotations: {
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: false,
  openWorldHint: true,
},
```

**resume_tool_operation.ts** (category: read — polls status of async job, no new mutations):
```typescript
annotations: {
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: false,
  openWorldHint: true,
},
```

**retrieve_metadata.ts** (category: read — writes local files but reads from org; destructiveHint:true because it overwrites local files):
```typescript
annotations: {
  readOnlyHint: true,
  destructiveHint: true,
  idempotentHint: false,
  openWorldHint: true,
},
```

**get_org_info.ts** (category: read — getName() returns 'salesforce_get_org_info', pure read):
```typescript
annotations: {
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: true,
  openWorldHint: false,
},
```

In each file, keep the existing annotations keys that are already present and add the missing ones. Do NOT add `title:` inside annotations — it belongs in the config root only. Replace the entire `annotations: { ... }` block to ensure consistency.
  </action>
  <verify>
    <automated>grep -c "idempotentHint" packages/mcp-provider-dx-core/src/tools/run_soql_query.ts packages/mcp-provider-dx-core/src/tools/list_all_orgs.ts packages/mcp-provider-dx-core/src/tools/get_username.ts packages/mcp-provider-dx-core/src/tools/run_apex_test.ts packages/mcp-provider-dx-core/src/tools/run_agent_test.ts packages/mcp-provider-dx-core/src/tools/resume_tool_operation.ts packages/mcp-provider-dx-core/src/tools/retrieve_metadata.ts packages/mcp-provider-dx-core/src/tools/get_org_info.ts</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 8 files contains exactly the 4 annotation keys: readOnlyHint, destructiveHint, idempotentHint, openWorldHint
    - All 8 tools have readOnlyHint: true (they are all 'read' category)
    - grep for "idempotentHint" returns at least one match in each of the 8 files
    - grep for "readOnlyHint: false" returns zero matches in all 8 files
  </acceptance_criteria>
  <done>All 8 dx-core read-category GA tools have complete 4-hint annotations with readOnlyHint: true</done>
</task>

<task type="auto">
  <name>Task 2: Complete annotations on dx-core write-category GA tools</name>
  <files>
    packages/mcp-provider-dx-core/src/tools/deploy_metadata.ts
    packages/mcp-provider-dx-core/src/tools/assign_permission_set.ts
  </files>
  <read_first>
    packages/mcp-provider-dx-core/src/tools/deploy_metadata.ts
    packages/mcp-provider-dx-core/src/tools/assign_permission_set.ts
  </read_first>
  <action>
Replace the `annotations:` block in each of the 2 write-category tools:

**deploy_metadata.ts** (category: write — deploys to org, irreversible without rollback; NOT idempotent — two deploys with same source can overwrite concurrent changes):
```typescript
annotations: {
  readOnlyHint: false,
  destructiveHint: true,
  idempotentHint: false,
  openWorldHint: true,
},
```

**assign_permission_set.ts** (category: write — modifies org user permissions; idempotent because assigning the same perm set twice is a no-op):
```typescript
annotations: {
  readOnlyHint: false,
  destructiveHint: false,
  idempotentHint: true,
  openWorldHint: true,
},
```

Replace the entire `annotations: { ... }` block in each file's `getConfig()` return value.
  </action>
  <verify>
    <automated>grep -A 6 "annotations:" packages/mcp-provider-dx-core/src/tools/deploy_metadata.ts packages/mcp-provider-dx-core/src/tools/assign_permission_set.ts</automated>
  </verify>
  <acceptance_criteria>
    - deploy_metadata.ts has readOnlyHint: false and destructiveHint: true
    - assign_permission_set.ts has readOnlyHint: false and openWorldHint: true
    - Both files contain all 4 annotation keys: readOnlyHint, destructiveHint, idempotentHint, openWorldHint
    - grep "readOnlyHint: true" returns zero matches in both files
  </acceptance_criteria>
  <done>Both dx-core write-category GA tools have complete 4-hint annotations with readOnlyHint: false</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotation values → LLM client | Client reads hints to decide confirmation dialog; wrong values mislead agent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | annotations block | accept | Values are static constants — no runtime input involved |
| T-06-02 | Information Disclosure | readOnlyHint:false on read tool | mitigate | Consistency test in Plan 03 catches any false-positive confirmation dialogs |
</threat_model>

<verification>
After completing both tasks, run:
```bash
cd /Users/dormon/Projects/Salesforce-MCP
grep -rn "idempotentHint" packages/mcp-provider-dx-core/src/tools/
```
Must show 10 matches (one per GA tool file that was modified).

```bash
grep -rn "readOnlyHint: false" packages/mcp-provider-dx-core/src/tools/
```
Must show only deploy_metadata.ts and assign_permission_set.ts.
</verification>

<success_criteria>
- All 10 dx-core GA tool files have 4-hint annotations blocks
- read-category tools: readOnlyHint: true
- write-category tools: readOnlyHint: false
- Zero tools missing idempotentHint
- TypeScript compiles without errors (tsc --noEmit in package)
</success_criteria>

<output>
After completion, create `.planning/phases/06-tool-annotations/06-01-SUMMARY.md`
</output>
